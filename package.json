{
  "name": "Row-node-project",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "SET NODE_ENV=staging && npx nodemon index.js",
    "production": "SET NODE_ENV=production && npx nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "eslint": "^8.55.0"
  }
}
